{
  "name": "hubspoke",
  "version": "1.0.0",
  "description": "HubSpoke scheduling app — root-level tooling scripts",
  "private": true,
  "scripts": {
    "seed": "node seed-demo-data.js"
  },
  "dependencies": {
    "mongodb": "^6.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}