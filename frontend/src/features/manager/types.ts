export type InviteFormState = { email: string; name: string; role: string };

export type LocationFormState = {
  city_name: string;
  drive_time_minutes: string;
  latitude: string;
  longitude: string;
};
