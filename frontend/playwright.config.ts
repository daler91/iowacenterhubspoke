import { defineConfig, devices } from '@playwright/test';

/**
 * Playwright config for end-to-end a11y + keyboard + mobile smoke tests.
 *
 * All specs run against a locally-booted Vite dev server. API calls are
 * intercepted by the fixture in `tests/e2e/fixtures.ts`, so no backend is
 * required — the suite should finish in well under a minute.
 */
export default defineConfig({
  testDir: './tests/e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  workers: process.env.CI ? 2 : undefined,
  reporter: [['html', { open: 'never' }], ['list']],
  use: {
    baseURL: 'http://localhost:3000',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
  webServer: {
    command: 'npm run start',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
});
